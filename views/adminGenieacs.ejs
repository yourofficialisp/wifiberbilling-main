<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title || 'Device GenieACS' %> - <%= typeof isTechnicianView !== 'undefined' && isTechnicianView ? 'Portal Technician' : 'Admin Panel' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/responsive/2.5.0/css/dataTables.responsive.min.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/responsive/2.5.0/css/responsive.bootstrap5.min.css" rel="stylesheet">
    <style>
        /* DataTables styling yang diperbaiki */
        .dataTables_wrapper .dataTables_length,
        .dataTables_wrapper .dataTables_filter,
        .dataTables_wrapper .dataTables_info,
        .dataTables_wrapper .dataTables_processing,
        .dataTables_wrapper .dataTables_paginate {
            margin: 10px 0;
            display: block !important;
            visibility: visible !important;
        }
        .dataTables_wrapper .dataTables_filter {
            float: right;
            text-align: right;
            margin-bottom: 10px;
        }
        .dataTables_wrapper .dataTables_filter input {
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 5px 10px;
            margin-left: 5px;
            width: 200px;
        }
        .dataTables_wrapper .dataTables_length {
            float: left;
            margin-bottom: 10px;
        }
        .dataTables_wrapper .dataTables_length select {
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 5px;
            margin: 0 5px;
        }
        .dataTables_wrapper .dataTables_paginate {
            float: right;
            margin-top: 10px;
        }
        .dataTables_wrapper .dataTables_paginate .paginate_button {
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 5px 10px;
            margin: 0 2px;
            cursor: pointer;
        }
        .dataTables_wrapper .dataTables_paginate .paginate_button.current {
            background: #007bff;
            color: white;
            border-color: #007bff;
        }
        .dataTables_wrapper .dataTables_info {
            clear: both;
            float: left;
            padding-top: 10px;
        }
        /* Ensure wrapper is visible */
        .dataTables_wrapper {
            display: block !important;
            visibility: visible !important;
        }
    </style>
    <link href="/css/responsive-admin.css" rel="stylesheet">
    <style>
        body { background: #f5f6fa; }
        .form-label { font-weight: 600; }
        .main-content {
            padding: 32px 24px;
        }
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        }
        /* Full width table container */
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            width: 100%;
            margin: 0;
        }
        .table {
            width: 100%;
            margin-bottom: 0;
            table-layout: auto;
        }
        /* Optimize column widths for better space usage */
        .table th:nth-child(1), .table td:nth-child(1) { width: 5%; min-width: 50px; }
        .table th:nth-child(2), .table td:nth-child(2) { width: 25%; min-width: 150px; }
        .table th:nth-child(3), .table td:nth-child(3) { width: 20%; min-width: 120px; }
        .table th:nth-child(4), .table td:nth-child(4) { width: 10%; min-width: 80px; text-align: center; }
        .table th:nth-child(5), .table td:nth-child(5) { width: 15%; min-width: 100px; }
        .table th:nth-child(6), .table td:nth-child(6) { width: 10%; min-width: 80px; text-align: center; }
        .table th:nth-child(7), .table td:nth-child(7) { width: 15%; min-width: 150px; }
        @media (max-width: 768px) {
            .table td, .table th {
                white-space: nowrap;
                min-width: 120px;
            }
            .table .btn {
                padding: 0.25rem 0.5rem;
                font-size: 0.75rem;
            }
        }
        /* Fix z-index untuk mobile sidebar */
        .main-content {
            position: relative;
            z-index: 1;
        }
        .card {
            position: relative;
            z-index: 1;
        }
        .table-responsive {
            position: relative;
            z-index: 1;
        }
        .btn {
            position: relative;
            z-index: 1;
        }
        
        /* Ensure mobile sidebar and overlay have correct z-index */
        @media (max-width: 767.98px) {
            .main-content {
                z-index: 1;
                margin-top: 70px !important; /* Extra space for mobile navbar */
                padding-top: 10px !important;
            }
            .card {
                z-index: 1;
            }
            /* Ensure cards do not overlap with mobile navbar */
            .row.mb-4:first-of-type {
                margin-top: 10px;
            }
        }
        
        /* Styling untuk tombol aksi */
        .btn-group-vertical {
            display: flex;
            flex-direction: column;
            gap: 2px;
        }
        .btn-group-vertical .btn {
            border-radius: 4px !important;
            margin: 0;
            font-size: 0.75rem;
            padding: 0.25rem 0.5rem;
            white-space: nowrap;
            min-width: 80px;
        }
        .btn-group-vertical .btn i {
            margin-right: 2px;
        }
        
        /* Responsive untuk tombol aksi */
        @media (max-width: 768px) {
            .btn-group-vertical .btn {
                font-size: 0.7rem;
                padding: 0.2rem 0.4rem;
                min-width: 70px;
            }
            .btn-group-vertical .btn i {
                margin-right: 1px;
            }
        }
        
        /* Ensure action column is not too narrow */
        .table th:last-child,
        .table td:last-child {
            min-width: 120px;
            width: 120px;
        }
        /* Styling untuk tombol aksi horizontal */
        .btn-group {
            display: flex;
            flex-direction: row;
            gap: 2px;
        }
        .btn-group .btn {
            border-radius: 4px !important;
            margin: 0;
            font-size: 0.75rem;
            padding: 0.25rem 0.5rem;
            white-space: nowrap;
            min-width: 70px;
            flex: 1;
        }
        .btn-group .btn:first-child {
            border-top-right-radius: 0 !important;
            border-bottom-right-radius: 0 !important;
        }
        .btn-group .btn:not(:first-child):not(:last-child) {
            border-radius: 0 !important;
        }
        .btn-group .btn:last-child {
            border-top-left-radius: 0 !important;
            border-bottom-left-radius: 0 !important;
        }
        
        /* Responsive untuk mobile */
        @media (max-width: 768px) {
            .btn-group .btn {
                font-size: 0.7rem;
                padding: 0.2rem 0.4rem;
                min-width: 60px;
            }
        }
        
        /* Comprehensive Mobile Responsive Design */
        @media (max-width: 767.98px) {
            /* Main content spacing for mobile */
            .main-content {
                margin-top: 70px !important;
                padding: 10px !important;
                margin-left: 0 !important;
                z-index: 1;
            }
            
            /* Stats cards responsive */
            .row.mb-4 .col-md-4 {
                margin-bottom: 1rem;
            }
            
            .card .card-body {
                padding: 1rem 0.75rem;
            }
            
            .card .fs-3 {
                font-size: 1.5rem !important;
            }
            
            .card .fs-2 {
                font-size: 1.3rem !important;
            }
            
            .card .fw-bold {
                font-size: 0.85rem;
            }
            
            /* Card header responsive */
            .card h4 {
                font-size: 1.1rem;
                margin-bottom: 1rem;
            }
            
            .card .mb-3 {
                margin-bottom: 1rem !important;
            }
            
            /* Table responsive improvements */
            .table-responsive {
                border: none;
                margin: 0 -0.5rem;
            }
            
            .table {
                font-size: 0.75rem;
                margin-bottom: 0;
            }
            
            .table th,
            .table td {
                padding: 0.4rem 0.25rem;
                white-space: nowrap;
                min-width: 80px;
                vertical-align: middle;
            }
            
            .table th:first-child,
            .table td:first-child {
                min-width: 40px;
                text-align: center;
            }
            
            .table th:nth-child(2),
            .table td:nth-child(2) {
                min-width: 100px;
                font-weight: 500;
            }
            
            .table th:nth-child(3),
            .table td:nth-child(3) {
                min-width: 80px;
            }
            
            .table th:nth-child(4),
            .table td:nth-child(4) {
                min-width: 60px;
                text-align: center;
            }
            
            .table th:nth-child(5),
            .table td:nth-child(5) {
                min-width: 90px;
            }
            
            .table th:nth-child(6),
            .table td:nth-child(6) {
                min-width: 70px;
                text-align: center;
            }
            
            .table th:last-child,
            .table td:last-child {
                min-width: 140px;
                width: 140px;
                position: sticky;
                right: 0;
                background-color: white;
                z-index: 2;
                border-left: 1px solid #dee2e6;
            }
            
            /* Action buttons mobile optimization */
            .btn-group {
                display: flex;
                flex-direction: column;
                gap: 1px;
                width: 100%;
            }
            
            .btn-group .btn {
                border-radius: 3px !important;
                font-size: 0.65rem;
                padding: 0.2rem 0.3rem;
                margin: 0;
                width: 100%;
                min-height: 28px;
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
                line-height: 1.2;
            }
            
            .btn-group .btn i {
                margin-right: 2px;
                font-size: 0.7rem;
            }
            
            /* DataTables mobile responsive */
            .dataTables_wrapper {
                margin: 0 -0.5rem;
            }
            
            .dataTables_wrapper .dataTables_length,
            .dataTables_wrapper .dataTables_filter {
                margin: 0.5rem 0;
                display: block;
                float: none !important;
                text-align: left !important;
            }
            
            .dataTables_wrapper .dataTables_filter input {
                width: 100% !important;
                max-width: 200px;
                margin: 0.25rem 0 0 0;
            }
            
            .dataTables_wrapper .dataTables_length select {
                margin: 0.25rem 0.5rem 0 0.5rem;
            }
            
            .dataTables_wrapper .dataTables_info {
                clear: both;
                float: none !important;
                text-align: center;
                font-size: 0.8rem;
                margin: 0.5rem 0;
            }
            
            .dataTables_wrapper .dataTables_paginate {
                float: none !important;
                text-align: center;
                margin: 0.5rem 0;
            }
            
            .dataTables_wrapper .dataTables_paginate .paginate_button {
                padding: 0.25rem 0.5rem;
                margin: 0 1px;
                font-size: 0.8rem;
                min-width: 30px;
            }
            
            /* Modal responsive */
            .modal-dialog {
                margin: 0.5rem;
                max-width: calc(100% - 1rem);
            }
            
            .modal-content {
                border-radius: 0.5rem;
            }
            
            .modal-header {
                padding: 0.75rem;
                border-bottom: 1px solid #dee2e6;
            }
            
            .modal-body {
                padding: 0.75rem;
            }
            
            .modal-footer {
                padding: 0.75rem;
                border-top: 1px solid #dee2e6;
            }
            
            .modal-title {
                font-size: 1rem;
            }
            
            /* Form controls in modal */
            .form-label {
                font-size: 0.9rem;
                font-weight: 600;
                margin-bottom: 0.25rem;
            }
            
            .form-control {
                padding: 0.5rem 0.75rem;
                font-size: 0.9rem;
                border-radius: 0.375rem;
                min-height: 38px;
            }
            
            /* Modal buttons */
            .modal .btn {
                padding: 0.5rem 1rem;
                font-size: 0.85rem;
                border-radius: 0.375rem;
                min-height: 38px;
                touch-action: manipulation;
            }
            
            .modal .btn.w-100 {
                margin-bottom: 0.5rem;
            }
            
            .modal .btn i {
                margin-right: 0.25rem;
            }
            
            /* Alert in modal */
            .modal .alert {
                padding: 0.5rem 0.75rem;
                margin-bottom: 0.75rem;
                font-size: 0.8rem;
                border-radius: 0.375rem;
            }
            
            .modal .alert i {
                margin-right: 0.25rem;
            }
            
            /* Toast notifications for mobile */
            .toast {
                max-width: calc(100vw - 2rem) !important;
                min-width: 250px !important;
                right: 1rem !important;
                top: 80px !important;
                font-size: 0.85rem;
            }
            
            .toast .toast-body {
                padding: 0.75rem;
                line-height: 1.4;
            }
            
            .toast .toast-body i {
                font-size: 1rem;
            }
            
            .toast .toast-body strong {
                font-size: 0.9rem;
            }
            
            .toast .toast-body small {
                font-size: 0.8rem;
                line-height: 1.3;
                display: block;
                margin-top: 0.25rem;
            }
            
            /* Touch-friendly interactions */
            .table tbody tr {
                min-height: 44px;
            }
            
            button, .btn {
                min-height: 38px;
                touch-action: manipulation;
                -webkit-tap-highlight-color: transparent;
            }
            
            /* Scrollbar styling for mobile */
            .table-responsive::-webkit-scrollbar {
                height: 4px;
            }
            
            .table-responsive::-webkit-scrollbar-track {
                background: #f1f1f1;
                border-radius: 2px;
            }
            
            .table-responsive::-webkit-scrollbar-thumb {
                background: #c1c1c1;
                border-radius: 2px;
            }
            
            .table-responsive::-webkit-scrollbar-thumb:hover {
                background: #a8a8a8;
            }
        }
        
        /* Medium screens (tablets) */
        @media (min-width: 768px) and (max-width: 991.98px) {
            .main-content {
                padding: 1.5rem;
            }
            
            .table {
                font-size: 0.85rem;
            }
            
            .btn-group .btn {
                font-size: 0.75rem;
                padding: 0.3rem 0.5rem;
            }
        }
        
        /* Ensure proper z-index layering */
        .table-responsive {
            position: relative;
            z-index: 1;
        }
        
        .main-content {
            position: relative;
            z-index: 1;
        }
        
        /* Better space utilization without breaking Bootstrap layout */
        @media (min-width: 768px) {
            .main-content {
                padding-left: 2rem;
                padding-right: 2rem;
            }
        }
        
        /* Ensure table uses available space properly */
        .card .table-responsive {
            margin: -1.25rem;
            margin-top: 0;
            border-radius: 0;
        }
        
        .card .table-responsive .table {
            margin-bottom: 0;
        }
        
        .card .card-body {
            padding-bottom: 0;
        }
        
        .card h4 {
            margin-bottom: 1.25rem;
        }
    </style>
</head>
<body>
<!-- Alert Container untuk Notifikasi -->
<div id="alert-container" style="position:fixed;top:70px;right:20px;z-index:1055;"></div>

<style>
/* Mobile alert positioning */
@media (max-width: 767.98px) {
    #alert-container {
        top: 80px !important; /* Below mobile navbar */
        right: 10px !important;
        left: 10px !important;
        width: auto !important;
    }
}
</style>

<div class="container-fluid">
    <div class="row">
        <!-- Include Responsive Sidebar -->
        <% if (typeof isTechnicianView !== 'undefined' && isTechnicianView) { %>
            <%- include('partials/technician-responsive-sidebar', { page: 'monitoring', technician: technician }) %>
        <% } else { %>
            <%- include('partials/admin-responsive-sidebar', { page: 'genieacs', settings: settings }) %>
        <% } %>

        <main class="col-md-10 main-content ms-sm-auto">
            <!-- GENIEACS CARDS: Statistik -->
            <div class="row mb-4">
              <div class="col-md-4 mb-3">
                <div class="card text-bg-primary">
                  <div class="card-body text-center">
                    <i class="bi bi-hdd-network fs-2 mb-2"></i>
                    <div class="fw-bold">Total Device GenieACS</div>
                    <div class="fs-3" id="genieacs-total"><%= typeof genieacsTotal !== 'undefined' ? genieacsTotal : '-' %></div>
                  </div>
                </div>
              </div>
              <div class="col-md-4 mb-3">
                <div class="card text-bg-success">
                  <div class="card-body text-center">
                    <i class="bi bi-wifi fs-2 mb-2"></i>
                    <div class="fw-bold">Device Online</div>
                    <div class="fs-3" id="genieacs-online"><%= typeof genieacsOnline !== 'undefined' ? genieacsOnline : '-' %></div>
                  </div>
                </div>
              </div>
              <div class="col-md-4 mb-3">
                <div class="card text-bg-danger">
                  <div class="card-body text-center">
                    <i class="bi bi-wifi-off fs-2 mb-2"></i>
                    <div class="fw-bold">Device Offline</div>
                    <div class="fs-3" id="genieacs-offline"><%= typeof genieacsOffline !== 'undefined' ? genieacsOffline : '-' %></div>
                  </div>
                </div>
              </div>
            </div>
            <div class="card mb-4">
                <div class="card-body">
                    <h4 class="mb-3"><i class="bi bi-hdd-network"></i> <%= title || 'List Device GenieACS' %></h4>
                    <div class="table-responsive">
                        <table id="genieacsTable" class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>No</th>
                                    <th>PPPoE Username</th>
                                    <th>SSID</th>
                                    <th>Connected Users</th>
                                    <th>Customer Number</th>
                                    <th>RXPower</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% devices.forEach(function(device, i) { %>
                                <tr>
                                    <td><%= i+1 %></td>
                                    <td><%= device.pppoeUsername %></td>
                                    <td><%= device.ssid %></td>
                                    <td><%= device.userKonek %></td>
                                    <td><%= device.tag %></td>
                                    <td><%= typeof device.rxPower !== 'undefined' ? device.rxPower : '-' %></td>
                                    <td>
                                        <div class="btn-group btn-group-sm" role="group">
                                            <button class="btn btn-warning edit-btn" data-id="<%= device.id %>" data-ssid="<%= device.ssid %>" data-password="<%= device.password %>" title="Edit SSID & Password"><i class="bi bi-pencil"></i> Edit SSID</button>
                                            <button class="btn btn-info edit-tag-btn" data-id="<%= device.id %>" data-tag="<%= device.tag || '' %>" title="Edit Tag"><i class="bi bi-tag"></i> Edit Tag</button>
                                            <button class="btn btn-danger restart-onu-btn" data-id="<%= device.id %>" data-serial="<%= device.serial %>" title="Restart ONU"><i class="bi bi-arrow-clockwise"></i> Restart</button>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
    </div>
</div>
<!-- Modal Edit SSID/Password -->
<div class="modal fade" id="editModal" tabindex="-1" aria-labelledby="editModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="editModalLabel">Edit SSID & Password</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div id="editAlert" style="display:none;"></div>
        <input type="hidden" id="deviceId">
        <div class="mb-3">
          <label for="ssid" class="form-label">SSID</label>
          <input type="text" class="form-control" id="ssid">
          <button type="button" class="btn btn-success mt-2 w-100" id="saveSsidBtn" data-bs-dismiss="modal">Save SSID</button>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="text" class="form-control" id="password">
          <button type="button" class="btn btn-primary mt-2 w-100" id="savePasswordBtn" data-bs-dismiss="modal">Save Password</button>
        </div>
      </div>
      <div class="modal-footer d-flex flex-column align-items-stretch gap-2">
        <div class="alert alert-info" role="alert">
          <i class="bi bi-info-circle"></i> <strong>Tips:</strong> After changing SSID/Password, restart the device to apply changes quickly.
        </div>
        <button type="button" class="btn btn-warning w-100" id="rebootAfterEditBtn">
          <i class="bi bi-arrow-clockwise"></i> Restart Device After Edit
        </button>
        <button type="button" class="btn btn-secondary w-100" data-bs-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

<!-- Scripts - only one set of jQuery and Bootstrap -->
<script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.5.0/js/dataTables.responsive.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.5.0/js/responsive.bootstrap5.min.js"></script>

<script>
$(document).ready(function(){
    console.log('Document ready, initializing DataTable...');
    
    // Initialize DataTables
    var table = $('#genieacsTable').DataTable({
        "responsive": true,
        "scrollX": true,
        "autoWidth": false,
        "lengthMenu": [[20, 50, 100, -1], [20, 50, 100, "All"]],
        "pageLength": 20,
        "language": {
            "search": "Search:",
            "lengthMenu": "Show _MENU_ data per page",
            "info": "Showing _START_ to _END_ of _TOTAL_ data",
            "infoEmpty": "Showing 0 to 0 of 0 data",
            "infoFiltered": "(filtered from _MAX_ total data)",
            "paginate": {
                "first": "First",
                "last": "Last",
                "next": "Next",
                "previous": "Previous"
            }
        },
        "columnDefs": [
            { "targets": [0], "responsivePriority": 1, "width": "5%" },
            { "targets": [1], "responsivePriority": 2, "width": "25%" },
            { "targets": [2], "responsivePriority": 4, "width": "20%" },
            { "targets": [3], "responsivePriority": 5, "width": "10%", "className": "text-center" },
            { "targets": [4], "responsivePriority": 6, "width": "15%" },
            { "targets": [5], "responsivePriority": 7, "width": "10%", "className": "text-center" },
            { "targets": [-1], "responsivePriority": 3, "orderable": false, "width": "15%" }
        ]
    });
    
    console.log('DataTable initialized successfully');
    
    // Event delegation tombol edit
    $(document).on('click', '.edit-btn', function(){
        const id = $(this).data('id');
        const ssid = $(this).data('ssid');
        const password = $(this).data('password');
        $('#deviceId').val(id);
        $('#ssid').val(ssid);
        $('#password').val(password);
        var modal = new bootstrap.Modal(document.getElementById('editModal'));
        modal.show();
    });

    // Alert function in modal
    function showEditAlert(type, message) {
        $('#editAlert').removeClass().addClass('alert alert-' + type).html(message).show();
    }

    // Save SSID button - Final Version
    $(document).on('click', '#saveSsidBtn', function(e){
        const id = $('#deviceId').val();
        const ssid = $('#ssid').val();
        
        // Validation
        if (!ssid || ssid.length < 3 || ssid.length > 32) {
            e.preventDefault();
            showEditAlert('danger', 'SSID must be 3-32 characters!');
            return;
        }

        // Show process notification after modal closes
        setTimeout(function() {
            showToast('info', 'Processing SSID...', `WiFi 2.4GHz: ${ssid} is being updated. Please wait...`);
        }, 300);
        
        // Send request
        $.ajax({
            url: '/admin/genieacs/edit',
            method: 'POST',
            data: { id, ssid },
            success: function(res) {
                if (res.success) {
                    showToast('success', 'SSID Updated!', `WiFi 2.4GHz: ${res.newSSID} updated successfully.`);
                    updateTableRow(id, 'ssid', ssid);
                } else {
                    showToast('error', 'Failed to Update SSID', res.message || 'Failed to update SSID!');
                }
            },
            error: function(err) {
                const errorMsg = err.responseJSON?.message || 'Failed to update SSID!';
                showToast('error', 'Error', errorMsg);
            }
        });
    });

    // Save Password button - Final Version
    $(document).on('click', '#savePasswordBtn', function(e){
        const id = $('#deviceId').val();
        const password = $('#password').val();
        
        // Validation
        if (!password || password.length < 8 || password.length > 63) {
            e.preventDefault();
            showEditAlert('danger', 'Password must be 8-63 characters!');
            return;
        }

        // Show process notification after modal closes
        setTimeout(function() {
            showToast('info', 'Processing Password...', 'WiFi password is being updated. Please wait...');
        }, 300);
        
        // Send request
        $.ajax({
            url: '/admin/genieacs/edit',
            method: 'POST',
            data: { id, password },
            success: function(res) {
                if (res.success) {
                    showToast('success', 'Password Updated!', 'WiFi password updated successfully.');
                } else {
                    showToast('error', 'Failed to Update Password', res.message || 'Failed to update password!');
                }
            },
            error: function(err) {
                const errorMsg = err.responseJSON?.message || 'Failed to update password!';
                showToast('error', 'Error', errorMsg);
            }
        });
    });

    // Restart device button after edit
    $('#rebootAfterEditBtn').on('click', function(e){
        e.preventDefault();
        const id = $('#deviceId').val();
        
        if (!id) {
            showEditAlert('danger', 'Device ID not found!');
            return;
        }

        const $btn = $(this);
        const originalText = $btn.html();
        $btn.prop('disabled', true).html('<i class="bi bi-hourglass-split"></i> Merestart...');
        
        $.ajax({
            url: '/admin/genieacs/restart-onu',
            method: 'POST',
            data: { id },
            success: function(res) {
                if (res.success) {
                    showEditAlert('success', 'Restart command sent successfully!');
                    showToast('success', 'Restart Successful', 'Device will restart in a few minutes to apply changes.');
                    setTimeout(function(){ $('#editModal').modal('hide'); }, 2000);
                } else {
                    showEditAlert('danger', res.message || 'Failed to restart device!');
                }
            },
            error: function(err) {
                const errorMsg = err.responseJSON?.message || 'Failed to restart device!';
                showEditAlert('danger', errorMsg);
                showToast('error', 'Error Restart', errorMsg);
            },
            complete: function() {
                $btn.prop('disabled', false).html(originalText);
            }
        });
    });

    // Modal return to first DataTables page when closed
    $('#editModal').on('hidden.bs.modal', function () {
        if (table && table.page) {
            table.page('first').draw('page');
        }
    });
    
    // Event handler for modal hidden - return to first DataTables page
    $('#editModal').on('hidden.bs.modal', function () {
        if (table && table.page) {
            table.page('first').draw('page');
        }
    });
    

    
    // Notification function
    function showAlert(message, type) {
        var alert = $(`<div class="alert alert-${type} alert-dismissible fade show" role="alert">
            ${message}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>`);
        $('#alert-container').append(alert);
        setTimeout(function(){ alert.alert('close'); }, 3000);
    }

    // Modern and reliable toast notification function
    function showToast(type, title, message, duration = 5000) {
        try {
            const toastId = 'toast-' + Date.now();
            const iconClass = type === 'success' ? 'bi-check-circle' : type === 'error' ? 'bi-x-circle' : type === 'info' ? 'bi-info-circle' : 'bi-hourglass-split';
            const bgClass = type === 'success' ? 'bg-success' : type === 'error' ? 'bg-danger' : type === 'info' ? 'bg-primary' : 'bg-warning';
            
            const toastHtml = `
                <div id="${toastId}" class="toast align-items-center text-white ${bgClass} border-0" role="alert" aria-live="assertive" aria-atomic="true" style="position: fixed; top: 20px; right: 20px; z-index: 9999; min-width: 300px;">
                    <div class="d-flex">
                        <div class="toast-body">
                            <i class="bi ${iconClass} me-2"></i>
                            <strong>${title}</strong><br>
                            <small>${message}</small>
                        </div>
                        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                    </div>
                </div>
            `;
            
            // Append to body directly if alert-container does not exist
            const container = $('#alert-container');
            if (container.length > 0) {
                container.append(toastHtml);
            } else {
                $('body').append(toastHtml);
            }
            
            const toastElement = document.getElementById(toastId);
            if (toastElement) {
                const toast = new bootstrap.Toast(toastElement, { delay: duration });
                toast.show();
                
                // Auto remove after hide
                toastElement.addEventListener('hidden.bs.toast', () => {
                    toastElement.remove();
                });
            }
            
            return toastId;
        } catch (error) {
            console.error('Error in showToast:', error);
            // Fallback to alert if toast fails
            alert(`${title}: ${message}`);
        }
    }



    // Fungsi update row di tabel tanpa reload
    function updateTableRow(deviceId, field, newValue) {
        try {
            // Find the row with the device ID
            table.rows().every(function(rowIdx, tableLoop, rowLoop) {
                const data = this.data();
                if (data && data.length > 0) {
                    // Check if this row contains the device ID (biasanya di kolom aksi)
                    const actionCell = data[data.length - 1]; // Last column is action
                    if (actionCell && actionCell.includes(deviceId)) {
                        if (field === 'ssid') {
                            // Update SSID column (index 2)
                            data[2] = newValue;
                            // Also update the SSID data attribute in action buttons
                            const updatedActionCell = actionCell.replace(/data-ssid="[^"]*"/, `data-ssid="${newValue}"`);
                            data[data.length - 1] = updatedActionCell;
                        }
                        this.data(data).draw(false);
                        return false; // Break the loop
                    }
                }
            });
        } catch (error) {
            console.error('Error updating table row:', error);
        }
    }
});
</script>

<!-- Modal Edit Tag -->
<div class="modal fade" id="editTagModal" tabindex="-1" aria-labelledby="editTagModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <form id="editTagForm">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editTagModalLabel">Edit Tag (Customer Number)</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <input type="hidden" id="tagDeviceId" name="id">
          <div class="mb-3">
            <label for="newTag" class="form-label">Customer Number</label>
            <input type="text" class="form-control" id="newTag" name="tag" required>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-primary">Save</button>
        </div>
      </div>
    </form>
  </div>
</div>

<!-- Modal Restart ONU -->
<div class="modal fade" id="restartOnuModal" tabindex="-1" aria-labelledby="restartOnuModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="restartOnuModalLabel">Confirm Restart</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="alert alert-warning" role="alert">
          <i class="bi bi-exclamation-triangle"></i> <strong>Warning!</strong>
        </div>
        <p>Are you sure you want to restart the device with Serial Number: <strong id="restartOnuSerial"></strong>?</p>
        <ul class="text-muted small">
          <li>ONU will temporarily disconnect from the network</li>
          <li>Customers will experience internet disruption during the restart process</li>
          <li>The restart process usually takes 1-2 minutes</li>
        </ul>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="button" class="btn btn-danger" id="confirmRestartOnu">
          <i class="bi bi-arrow-clockwise"></i> Yes, Restart
        </button>
      </div>
    </div>
  </div>
</div>

<script>
$(document).ready(function() {
  // Tombol Edit Tag
  $(document).on('click', '.edit-tag-btn', function() {
    const id = $(this).data('id');
    const tag = $(this).data('tag');
    $('#tagDeviceId').val(id);
    $('#newTag').val(tag !== '-' ? tag : '');
    $('#editTagModal').modal('show');
  });

  // Submit Edit Tag
  $('#editTagForm').submit(function(e) {
    e.preventDefault();
    const id = $('#tagDeviceId').val();
    const tag = $('#newTag').val();
    $.ajax({
      url: '/admin/genieacs/edit-tag',
      type: 'POST',
      data: { id, tag },
      success: function(res) {
        if (res.success) {
          location.reload();
        } else {
          alert('Failed to update tag: ' + (res.message || 'Unknown error'));
        }
      },
      error: function(xhr) {
        alert('Failed to update tag: ' + (xhr.responseJSON && xhr.responseJSON.message ? xhr.responseJSON.message : 'Unknown error'));
      }
    });
  });

  // Tombol Restart ONU
  $(document).on('click', '.restart-onu-btn', function() {
    const id = $(this).data('id');
    const serial = $(this).data('serial');
    $('#restartOnuSerial').text(serial || id);
    $('#confirmRestartOnu').data('id', id);
    $('#restartOnuModal').modal('show');
  });

  // Confirm Restart ONU
  $('#confirmRestartOnu').on('click', function() {
    const id = $(this).data('id');
    const $btn = $(this);
    const originalText = $btn.html();
    
    // Loading state
    $btn.prop('disabled', true).html('<i class="bi bi-hourglass-split"></i> Restarting...');
    
    $.ajax({
      url: '/admin/genieacs/restart-onu',
      type: 'POST',
      data: { id },
      success: function(res) {
        if (res.success) {
          $('#restartOnuModal').modal('hide');
          // Show success alert
          const alertHtml = `
            <div class="alert alert-success alert-dismissible fade show" role="alert">
              <i class="bi bi-check-circle"></i> ${res.message}
              <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
          `;
          $('.main-content').prepend(alertHtml);
          // Auto-hide alert after 5 seconds
          setTimeout(() => {
            $('.alert-success').fadeOut();
          }, 5000);
        } else {
          alert('Failed to restart: ' + (res.message || 'Unknown error'));
        }
      },
      error: function(xhr) {
        const errorMsg = xhr.responseJSON && xhr.responseJSON.message ? xhr.responseJSON.message : 'Unknown error';
        alert('Failed to restart: ' + errorMsg);
      },
      complete: function() {
        // Reset button
        $btn.prop('disabled', false).html(originalText);
      }
    });
  });
});
</script>



<!-- Modal Confirm Restart Mikrotik -->
<div class="modal fade" id="restartMikrotikModal" tabindex="-1" aria-labelledby="restartMikrotikModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="restartMikrotikModalLabel"><i class="bi bi-arrow-repeat"></i> Confirm Mikrotik Restart</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Are you sure you want to <b>restart Mikrotik</b>?<br>The router will reboot and customer internet connections will be temporarily interrupted.
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="button" class="btn btn-danger" id="confirmRestartMikrotik">Restart</button>
      </div>
    </div>
  </div>
</div>

<!-- Notification -->
<div id="restartNotif" class="alert d-none position-fixed top-0 end-0 m-4" style="z-index:9999; min-width:300px;"></div>

<script>
$(function() {
  $(document).on('click', '#restartMikrotikBtn', function(e) {
    e.preventDefault();
    $('#restartMikrotikModal').modal('show');
  });
  $('#confirmRestartMikrotik').on('click', function() {
    $('#restartMikrotikModal').modal('hide');
    $.ajax({
      url: '/admin/mikrotik/restart',
      method: 'POST',
      success: function(res) {
        let notif = $('#restartNotif');
        if(res.success) {
          notif.removeClass('d-none alert-danger').addClass('alert-success').text(res.message || 'Mikrotik restarted successfully!');
        } else {
          notif.removeClass('d-none alert-success').addClass('alert-danger').text(res.message || 'Failed to restart Mikrotik!');
        }
        setTimeout(function() { notif.addClass('d-none'); }, 4000);
      },
      error: function() {
        let notif = $('#restartNotif');
        notif.removeClass('d-none alert-success').addClass('alert-danger').text('Failed to contact server!');
        setTimeout(function() { notif.addClass('d-none'); }, 4000);
      }
    });
  });
});

</script>

<!-- Mobile Bottom Navigation -->
<% if (typeof isTechnicianView !== 'undefined' && isTechnicianView) { %>
    <%- include('partials/mobile-navbar', { currentPage: 'dashboard' }) %>

<!-- Add bottom padding for mobile -->
<style>
    @media (max-width: 768px) {
        body {
            padding-bottom: 80px !important;
        }
        
        .main-content {
            padding-bottom: 80px !important;
        }
        
        .dataTables_wrapper {
            margin-bottom: 80px !important;
        }
    }
</style>
<% } %>

</body>
</html>
