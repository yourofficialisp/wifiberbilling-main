<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Voucher Not Found - <%= settings.company_header || 'GEMBOK-BILL' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2255a6;
            --secondary-color: #f8f9fa;
            --success-color: #28a745;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --info-color: #17a2b8;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }

        .error-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .error-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            border: none;
            max-width: 600px;
            width: 100%;
            text-align: center;
            padding: 40px;
        }

        .error-icon {
            font-size: 4rem;
            color: var(--danger-color);
            margin-bottom: 20px;
        }

        .btn-custom {
            background: linear-gradient(135deg, var(--primary-color), #6c5ce7);
            border: none;
            border-radius: 10px;
            padding: 12px 30px;
            color: white;
            font-weight: 500;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
        }

        .btn-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
            color: white;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-card">
            <div class="error-icon">
                <i class="bi bi-exclamation-triangle-fill"></i>
            </div>

            <h1 class="h3 mb-3">Voucher Not Found</h1>

            <p class="text-muted mb-4">
                Sorry, the voucher you are looking for cannot be found in our system.
            </p>

            <div class="alert alert-info text-start mb-4">
                <h6><i class="bi bi-info-circle-fill me-2"></i>Possible causes:</h6>
                <ul class="mb-3">
                    <li><strong>Invalid voucher ID</strong> - Make sure the link you use is correct</li>
                    <li><strong>Voucher has expired</strong> - Vouchers are usually active 24 hours after purchase</li>
                    <li><strong>Payment link expired</strong> - Payment links have a time limit</li>
                    <li><strong>Data already processed</strong> - Voucher may have been used or sent</li>
                    <li><strong>Temporary technical problem</strong> - Try again in a moment</li>
                </ul>

                <div class="border-top pt-3">
                    <h6><i class="bi bi-lightbulb-fill me-2"></i>Solutions to try:</h6>
                    <ul class="mb-0">
                        <li>Check email or WhatsApp again for the correct link</li>
                        <li>If you just paid, wait 1-2 minutes then try again</li>
                        <li>Contact admin if you experience repeated difficulties</li>
                    </ul>
                </div>
            </div>

            <div class="d-grid gap-3">
                <a href="/voucher" class="btn-custom">
                    <i class="bi bi-cart-plus me-2"></i>Buy New Voucher
                </a>

                <a href="/" class="btn btn-outline-secondary">
                    <i class="bi bi-house me-2"></i>Back to Home
                </a>
            </div>

            <% if (typeof voucherStats !== 'undefined' && voucherStats && (voucherStats.total_today > 0)) { %>
            <div class="alert alert-light border mt-4">
                <div class="row text-center">
                    <div class="col-4">
                        <div class="h5 mb-1 text-primary"><%= voucherStats.total_today %></div>
                        <small class="text-muted">Total Orders<br>Today</small>
                    </div>
                    <div class="col-4">
                        <div class="h5 mb-1 text-success"><%= voucherStats.completed_today %></div>
                        <small class="text-muted">Successfully<br>Sent</small>
                    </div>
                    <div class="col-4">
                        <div class="h5 mb-1 text-warning"><%= voucherStats.pending_today %></div>
                        <small class="text-muted">Waiting for<br>Payment</small>
                    </div>
                </div>
            </div>

            <% if (typeof recentVouchers !== 'undefined' && recentVouchers && recentVouchers.length > 0) { %>
            <div class="alert alert-success mt-3">
                <h6><i class="bi bi-check-circle-fill me-2"></i>Latest Vouchers Today:</h6>
                <div class="table-responsive">
                    <table class="table table-sm mb-0">
                        <thead>
                            <tr>
                                <th>Package</th>
                                <th>Customer</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% recentVouchers.forEach(voucher => { %>
                            <tr>
                                <td><small><%= voucher.voucher_package %>k x<%= voucher.voucher_quantity %></small></td>
                                <td><small><%= voucher.customer_name || voucher.customer_name %></small></td>
                                <td><small><%= new Date(voucher.created_at).toLocaleString('en-PK') %></small></td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                <small class="text-muted mt-2 d-block">
                    The voucher system is running properly! If your voucher does not appear, it may still be being processed.
                </small>
            </div>
            <% } %>
            <% } %>

            <div class="mt-4 pt-4 border-top">
                <div class="row text-center">
                    <div class="col-md-6 mb-3">
                        <h6><i class="bi bi-whatsapp text-success me-2"></i>WhatsApp Support</h6>
                        <a href="https://wa.me/923036783333" class="btn btn-success btn-sm" target="_blank">
                            <i class="bi bi-whatsapp me-1"></i>Chat Admin
                        </a>
                        <div class="small text-muted mt-1">03036783333</div>
                    </div>
                    <div class="col-md-6 mb-3">
                        <h6><i class="bi bi-clock text-info me-2"></i>Operating Hours</h6>
                        <div class="small text-muted">Monday - Sunday<br>08:00 - 22:00 PKT</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Check if there is voucher data in localStorage
        document.addEventListener('DOMContentLoaded', function() {
            const lastPurchase = localStorage.getItem('lastVoucherPurchase');
            if (lastPurchase) {
                try {
                    const purchaseData = JSON.parse(lastPurchase);
                    const purchaseTime = new Date(purchaseData.timestamp);
                    const now = new Date();
                    const diffHours = (now - purchaseTime) / (1000 * 60 * 60);

                    // If purchase within last 24 hours, show info
                    if (diffHours < 24) {
                        const infoAlert = document.createElement('div');
                        infoAlert.className = 'alert alert-warning mt-3';
                        infoAlert.innerHTML = `
                            <h6><i class="bi bi-info-circle-fill me-2"></i>Last Purchase Data Found:</h6>
                            <p class="mb-2">
                                <strong>Package:</strong> ${purchaseData.packageName}<br>
                                <strong>Time:</strong> ${purchaseTime.toLocaleString('en-PK')}<br>
                                <strong>Status:</strong> ${purchaseData.status || 'Waiting for Payment'}
                            </p>
                            <small class="text-muted">
                                If you just made a payment, wait 1-2 minutes then refresh this page.
                            </small>
                        `;

                        document.querySelector('.error-card').insertBefore(infoAlert, document.querySelector('.error-card .alert-info'));
                    }
                } catch (e) {
                    console.log('Error parsing last purchase data:', e);
                }
            }
        });

        // Function to refresh page
        function refreshPage() {
            window.location.reload();
        }

        // Auto-refresh every 30 seconds if user just purchased
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.has('from_payment')) {
            let countdown = 30;
            const countdownElement = document.createElement('div');
            countdownElement.className = 'alert alert-primary mt-3';
            countdownElement.innerHTML = `
                <i class="bi bi-arrow-clockwise me-2"></i>
                Looking for your voucher... Page will refresh automatically in <strong>${countdown}</strong> seconds.
                <button onclick="refreshPage()" class="btn btn-sm btn-primary ms-2">Refresh Now</button>
            `;

            document.querySelector('.error-card').insertBefore(countdownElement, document.querySelector('.error-card .d-grid'));

            const countdownInterval = setInterval(() => {
                countdown--;
                countdownElement.querySelector('strong').textContent = countdown;

                if (countdown <= 0) {
                    clearInterval(countdownInterval);
                    refreshPage();
                }
            }, 1000);
        }
    </script>
</body>
</html>
