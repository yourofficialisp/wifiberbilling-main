<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Internet Packages - Admin Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.0.7/css/boxicons.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    <!-- Admin Mobile Optimizations -->
    <link href="/css/admin-mobile.css" rel="stylesheet">
    <link href="/css/mobile-sidebar.css" rel="stylesheet">
    <link href="/css/responsive-admin.css" rel="stylesheet">
    <style>
        .toast {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
            min-width: 300px;
        }
    </style>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <%- include('../../partials/billing-sidebar') %>

                <!-- Main content -->
                <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
                    <h3 class="mb-4">Customers & Packages</h3>

                    <!-- Tab Navigation -->
                    <ul class="nav nav-tabs mb-4">
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'customers' ? 'active' : '' %>"
                                href="/admin/billing/customers">
                                <i class="bi bi-people"></i> Customers
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'packages' ? 'active' : '' %>"
                                href="/admin/billing/packages">
                                <i class="bi bi-box"></i> Packages
                            </a>
                        </li>
                    </ul>

                    <div
                        class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                        <h1 class="h2">
                            <i class="bx bx-package"></i> Manage Internet Packages
                        </h1>
                        <div>
                            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addPackageModal">
                                <i class="bx bx-plus"></i> Add Package
                            </button>
                        </div>
                    </div>

                    <!-- Packages Table -->
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title mb-0">
                                <i class="bx bx-list-ul"></i> Package List
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table id="packagesTable" class="table table-striped table-hover">
                                    <thead>
                                        <tr>
                                            <th>Package Name</th>
                                            <th>Speed</th>
                                            <th>Price (Before VAT)</th>
                                            <th>VAT</th>
                                            <th>Price (After VAT)</th>
                                            <th>PPPoE Profileeeeeeeeee</th>
                                            <th>Description</th>
                                            <th>Status</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% if (packages && packages.length> 0) { %>
                                            <% packages.forEach(function(package) { %>
                                                <tr>
                                                    <td>
                                                        <span class="text-body fw-bold">
                                                            <%= package.name %>
                                                        </span>
                                                        <% if (package.image_filename) { %>
                                                            <br><img src="/img/packages/<%= package.image_filename %>"
                                                                alt="<%= package.name %>"
                                                                style="width: 50px; height: 50px; object-fit: cover; margin-top: 5px; border-radius: 5px;">
                                                            <% } %>
                                                    </td>
                                                    <td>
                                                        <%= package.speed %>
                                                    </td>
                                                    <td>Rs <%= parseFloat(package.price).toLocaleString('en-PK') %>
                                                    </td>
                                                    <td>
                                                        <% if (package.tax_rate===0) { %>
                                                            <span class="badge bg-secondary">0% (No VAT)</span>
                                                            <% } else if (package.tax_rate) { %>
                                                                <span class="badge bg-warning">
                                                                    <%= package.tax_rate.toFixed(2) %>%
                                                                </span>
                                                                <% } else { %>
                                                                    <span class="badge bg-warning">11.00%</span>
                                                                    <% } %>
                                                    </td>
                                                    <td class="text-success fw-bold">
                                                        <% if (package.tax_rate===0) { %>
                                                            Rs <%= parseFloat(package.price).toLocaleString('en-PK') %>
                                                                <small class="text-muted d-block">(Same price, no VAT)</small>
                                                                <% } else if (package.tax_rate) { %>
                                                                    Rp <%= ((parseFloat(package.price) * (1 +
                                                                        package.tax_rate /
                                                                        100)).toFixed(0)).toLocaleString('en-PK') %>
                                                                        <% } else { %>
                                                                            Rs <%= ((parseFloat(package.price) *
                                                                                1.11).toFixed(0)).toLocaleString('en-PK')
                                                                                %>
                                                                                <% } %>
                                                    </td>
                                                    <td><span class="badge bg-info">
                                                            <%= package.pppoe_profile || 'default' %>
                                                        </span></td>
                                                    <td>
                                                        <%= package.description || '-' %>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-success">Active</span>
                                                    </td>
                                                    <td>
                                                        <div class="btn-group" role="group">
                                                            <button class="btn btn-sm btn-primary"
                                                                onclick="editPackage('<%= package.id %>')">
                                                                <i class="bx bx-edit"></i>
                                                            </button>
                                                            <button class="btn btn-sm btn-info"
                                                                onclick="viewPackage('<%= package.id %>')">
                                                                <i class="bx bx-show"></i>
                                                            </button>
                                                            <button class="btn btn-sm btn-danger"
                                                                onclick="deletePackage('<%= package.id %>')">
                                                                <i class="bx bx-trash"></i>
                                                            </button>
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% }); %>
                                                    <% } %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </main>
        </div>
    </div>

    <!-- Add Package Modal -->
    <div class="modal fade" id="addPackageModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Add New Package</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form id="addPackageForm">
                    <div class="modal-body">
                        <div class="mb-3">
                            <label for="name" class="form-label">Package Name</label>
                            <input type="text" class="form-control" id="name" name="name" required>
                        </div>
                        <div class="mb-3">
                            <label for="speed" class="form-label">Speed</label>
                            <input type="text" class="form-control" id="speed" name="speed"
                                placeholder="Example: 10 Mbps" required>
                        </div>
                        <div class="mb-3">
                            <label for="price" class="form-label">Price (Before VAT)</label>
                            <input type="number" class="form-control" id="price" name="price" required>
                        </div>
                        <div class="mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="enable_tax" name="enable_tax">
                                <label class="form-check-label" for="enable_tax">
                                    Enable VAT
                                </label>
                                <small class="form-text text-muted d-block">Check to enable VAT, uncheck for no VAT</small>
                            </div>
                        </div>
                        <div class="mb-3" id="tax_rate_group">
                            <label for="tax_rate" class="form-label">VAT (%)</label>
                            <input type="number" class="form-control" id="tax_rate" name="tax_rate" step="0.01" min="0"
                                max="100" value="0">
                            <small class="form-text text-muted">VAT percentage to be added to price
                                package.</small>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Price After VAT</label>
                            <div class="input-group">
                                <span class="input-group-text">Rp</span>
                                <input type="text" class="form-control" id="price_with_tax" readonly>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="description" class="form-label">Description</label>
                            <textarea class="form-control" id="description" name="description" rows="3"></textarea>
                        </div>
                        <div class="mb-3">
                            <label for="pppoe_profile" class="form-label">PPPoE Profileeeeeeeeee</label>
                            <div class="input-group">
                                <select class="form-select" id="pppoe_profile" name="pppoe_profile">
                                    <option value="default">default</option>
                                </select>
                                <button class="btn btn-outline-secondary" type="button"
                                    id="reloadProfileeeeeeeeeesAdd">Reload</button>
                            </div>
                            <small class="form-text text-muted">Select PPPoE profile from Mikrotik. Click Reload if list doesn't appear.</small>
                        </div>
                        <div class="mb-3">
                            <label for="package_image" class="form-label">Package Image (Optional)</label>
                            <input type="file" class="form-control" id="package_image" name="package_image"
                                accept="image/png, image/jpeg, image/jpg">
                            <small class="form-text text-muted">Supported formats: PNG, JPG, JPEG. Maximum 2MB.</small>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary">Save Package</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Edit Package Modal -->
    <div class="modal fade" id="editPackageModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Edit Package</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form id="editPackageForm">
                    <div class="modal-body">
                        <input type="hidden" id="edit_package_id" name="package_id">
                        <div class="mb-3">
                            <label for="edit_name" class="form-label">Package Name</label>
                            <input type="text" class="form-control" id="edit_name" name="name" required>
                        </div>
                        <div class="mb-3">
                            <label for="edit_speed" class="form-label">Speed</label>
                            <input type="text" class="form-control" id="edit_speed" name="speed" required>
                        </div>
                        <div class="mb-3">
                            <label for="edit_price" class="form-label">Price (Before VAT)</label>
                            <input type="number" class="form-control" id="edit_price" name="price" required>
                        </div>
                        <div class="mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="edit_enable_tax" name="enable_tax">
                                <label class="form-check-label" for="edit_enable_tax">
                                    Enable VAT
                                </label>
                                <small class="form-text text-muted d-block">Check to enable VAT, uncheck for no VAT</small>
                            </div>
                        </div>
                        <div class="mb-3" id="edit_tax_rate_group">
                            <label for="edit_tax_rate" class="form-label">VAT (%)</label>
                            <input type="number" class="form-control" id="edit_tax_rate" name="tax_rate" step="0.01"
                                min="0" max="100">
                            <small class="form-text text-muted">VAT percentage to be added to price
                                package.</small>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Price After VAT</label>
                            <div class="input-group">
                                <span class="input-group-text">Rp</span>
                                <input type="text" class="form-control" id="edit_price_with_tax" readonly>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="edit_description" class="form-label">Description</label>
                            <textarea class="form-control" id="edit_description" name="description" rows="3"></textarea>
                        </div>
                        <div class="mb-3">
                            <label for="edit_pppoe_profile" class="form-label">PPPoE Profileeeeeeeeee</label>
                            <div class="input-group">
                                <select class="form-select" id="edit_pppoe_profile" name="pppoe_profile">
                                    <option value="default">default</option>
                                </select>
                                <button class="btn btn-outline-secondary" type="button"
                                    id="reloadProfileeeeeeeeeesEdit">Reload</button>
                            </div>
                            <small class="form-text text-muted">Select PPPoE profile from Mikrotik. Click Reload if list doesn't appear.</small>
                        </div>
                        <div class="mb-3">
                            <label for="edit_package_image" class="form-label">Package Image (Optional)</label>
                            <input type="file" class="form-control" id="edit_package_image" name="package_image"
                                accept="image/png, image/jpeg, image/jpg">
                            <small class="form-text text-muted">Supported formats: PNG, JPG, JPEG. Maximum 2MB.</small>
                            <div id="current_image_preview" class="mt-2"></div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary">Update Package</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- View Package Modal -->
    <div class="modal fade" id="viewPackageModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Package Details</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" id="packageDetails">
                    <!-- Package details will be loaded here -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#packagesTable').DataTable({
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/1.13.6/i18n/id.json',
                    emptyTable: "No data available",
                    info: "Showing _START_ to _END_ of _TOTAL_ entries",
                    infoEmpty: "Showing 0 to 0 of 0 entries",
                    infoFiltered: "(filtered from _MAX_ total entries)",
                    lengthMenu: "Show _MENU_ entries",
                    loadingRecords: "Loading...",
                    processing: "Processing...",
                    search: "Search:",
                    zeroRecords: "No matching records found",
                    paginate: {
                        first: "First",
                        last: "Last",
                        next: "Next",
                        previous: "Previous"
                    }
                },
                pageLength: 25,
                lengthMenu: [[10, 25, 50, 100], [10, 25, 50, 100]]
            });

            // Load profiles into Add form on open
            $('#addPackageModal').on('shown.bs.modal', function () {
                console.log('🔍 Modal opened - resetting form...');
                loadPPPoEProfileeeeeeeeees('#pppoe_profile');
                // Reset form and hide tax group
                $('#addPackageForm')[0].reset();
                $('#enable_tax').prop('checked', false);
                $('#tax_rate_group').hide();
                $('#tax_rate_group input').prop('disabled', true);
                $('#tax_rate').val(0);
                // Ensure price with tax shows base price
                const price = parseFloat($('#price').val()) || 0;
                $('#price_with_tax').val(price.toLocaleString('en-PK'));

                console.log('   - Form reset completed');
                console.log('   - Checkbox checked:', $('#enable_tax').is(':checked'));
                console.log('   - Tax rate value:', $('#tax_rate').val());
                console.log('   - Tax rate disabled:', $('#tax_rate').prop('disabled'));
            });
            // Preload on page ready for Add form
            loadPPPoEProfileeeeeeeeees('#pppoe_profile');
            // Manual reload buttons
            $('#reloadProfileeeeeeeeeesAdd').on('click', function () { loadPPPoEProfileeeeeeeeees('#pppoe_profile'); });
            $('#reloadProfileeeeeeeeeesEdit').on('click', function () { loadPPPoEProfileeeeeeeeees('#edit_pppoe_profile'); });

            // Initialize VAT calculation
            initializeVATCalculation();

            // Set default tax rate from settings if available
            setDefaultTaxRate();

            // Hide tax rate group by default (checkbox unchecked)
            $('#tax_rate_group').hide();
            $('#tax_rate_group input').prop('disabled', true);

            // Hide edit tax rate group by default (checkbox unchecked)
            $('#edit_tax_rate_group').hide();
            $('#edit_tax_rate_group input').prop('disabled', true);
        });

        // VAT Calculation Functions
        function initializeVATCalculation() {
            // Add Package Modal VAT calculation
            $('#price, #tax_rate').on('input', function () {
                calculatePriceWithTax('price', 'tax_rate', 'price_with_tax');
            });

            // Edit Package Modal VAT calculation
            $('#edit_price, #edit_tax_rate').on('input', function () {
                calculatePriceWithTax('edit_price', 'edit_tax_rate', 'edit_price_with_tax');
            });

            // Checkbox VAT event handlers
            $('#enable_tax').on('change', function () {
                toggleTaxRateGroup('tax_rate_group', this.checked);
                calculatePriceWithTax('price', 'tax_rate', 'price_with_tax');
            });

            $('#edit_enable_tax').on('change', function () {
                toggleTaxRateGroup('edit_tax_rate_group', this.checked);
                calculatePriceWithTax('edit_price', 'edit_tax_rate', 'edit_price_with_tax');
            });
        }

        function toggleTaxRateGroup(groupId, enabled) {
            const group = $('#' + groupId);
            const inputs = group.find('input, select');

            if (enabled) {
                group.show();
                inputs.prop('disabled', false);
            } else {
                group.hide();
                inputs.prop('disabled', true);
                // Set tax rate to 0 when disabled
                if (groupId === 'tax_rate_group') {
                    $('#tax_rate').val(0);
                } else {
                    $('#edit_tax_rate').val(0);
                }
            }
        }

        function calculatePriceWithTax(priceId, taxId, resultId) {
            const price = parseFloat($('#' + priceId).val()) || 0;
            let taxRate = 0;

            // Check if tax is enabled
            if (priceId === 'price') {
                taxRate = $('#enable_tax').is(':checked') ? (parseFloat($('#' + taxId).val()) || 0) : 0;
            } else {
                taxRate = $('#edit_enable_tax').is(':checked') ? (parseFloat($('#' + taxId).val()) || 0) : 0;
            }

            // If VAT = 0 or not enabled, price doesn't change
            if (taxRate === 0) {
                $('#' + resultId).val(Math.round(price).toLocaleString('en-PK'));
                // Add class or style to show no VAT
                $('#' + resultId).addClass('text-muted');
                // Add tooltip or info
                $('#' + resultId).attr('title', 'No VAT (0%)');
            } else {
                const priceWithTax = price * (1 + taxRate / 100);
                $('#' + resultId).val(Math.round(priceWithTax).toLocaleString('en-PK'));
                // Remove class and tooltip
                $('#' + resultId).removeClass('text-muted');
                $('#' + resultId).removeAttr('title');
            }
        }

        function setDefaultTaxRate() {
            // Set tax rate to 0 by default (no VAT unless explicitly enabled)
            $('#tax_rate').val(0);
            $('#edit_tax_rate').val(0);

            // Don't trigger calculation since checkbox is unchecked by default
            console.log('Tax rate set to 0% by default (no VAT)');
        }

        // Fetch and populate PPPoE Profileeeeeeeeees from Mikrotik
        function loadPPPoEProfileeeeeeeeees(selectSelector, selectedValue = 'default') {
            const sel = document.querySelector(selectSelector);
            if (!sel) return;
            // show loading option
            sel.innerHTML = '<option>Loading...</option>';
            fetch('/admin/mikrotik/profiles/api')
                .then(r => r.json())
                .then(data => {
                    const profiles = (data && data.success && Array.isArray(data.profiles)) ? data.profiles : [];
                    // Build unique name list
                    const names = Array.from(new Set(profiles.map(p => p.name).filter(Boolean)));
                    // Fallback ensure 'default' exists
                    if (!names.includes('default')) names.unshift('default');
                    sel.innerHTML = '';
                    names.forEach(name => {
                        const opt = document.createElement('option');
                        opt.value = name;
                        opt.textContent = name;
                        sel.appendChild(opt);
                    });
                    sel.value = selectedValue || 'default';
                    // if selected not in list, append and select it
                    if (sel.value !== (selectedValue || 'default')) {
                        const opt = document.createElement('option');
                        opt.value = selectedValue;
                        opt.textContent = selectedValue;
                        sel.appendChild(opt);
                        sel.value = selectedValue;
                    }
                })
                .catch(err => {
                    console.error('Failed to load PPPoE profiles:', err);
                    if (typeof showAlert === 'function') {
                        showAlert('danger', 'Failed to load PPPoE profiles from Mikrotik');
                    }
                    // at least fallback
                    sel.innerHTML = '<option value="default">default</option>';
                    sel.value = selectedValue || 'default';
                });
        }

        // Add Package Form
        $('#addPackageForm').on('submit', function (e) {
            e.preventDefault();

            // Get form data
            const formData = new FormData();

            // Add text fields
            formData.append('name', $('#name').val().trim());
            formData.append('speed', $('#speed').val().trim());
            formData.append('price', parseFloat($('#price').val()));
            formData.append('tax_rate', $('#enable_tax').is(':checked') ? parseFloat($('#tax_rate').val()) : 0);
            formData.append('description', $('#description').val().trim());
            formData.append('pppoe_profile', $('#pppoe_profile').val().trim() || 'default');

            // Add image file if selected
            const imageFile = $('#package_image')[0].files[0];
            if (imageFile) {
                formData.append('package_image', imageFile);
            }

            // Debug logging
            console.log('🔍 Form submission debug:');
            console.log('   - Checkbox checked:', $('#enable_tax').is(':checked'));
            console.log('   - Tax rate input value:', $('#tax_rate').val());
            console.log('   - Tax rate input disabled:', $('#tax_rate').prop('disabled'));
            console.log('   - Calculated tax_rate:', formData.get('tax_rate'));

            // Validate required fields
            if (!formData.get('name') || !formData.get('speed') || !formData.get('price') || formData.get('price') <= 0) {
                showAlert('danger', 'Name, speed, and price must be filled correctly');
                return;
            }

            // Validate tax rate if enabled
            if ($('#enable_tax').is(':checked') && (parseFloat(formData.get('tax_rate')) < 0 || parseFloat(formData.get('tax_rate')) > 100)) {
                showAlert('danger', 'VAT must be between 0% and 100%');
                return;
            }

            // Show loading state
            const submitBtn = $(this).find('button[type="submit"]');
            const originalText = submitBtn.text();
            submitBtn.prop('disabled', true).text('Processing...');

            fetch('/admin/billing/packages', {
                method: 'POST',
                body: formData
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        showAlert('success', 'Package successfully added');
                        // Close modal with a small delay to ensure toast is shown
                        setTimeout(() => {
                            $('#addPackageModal').modal('hide');
                            setTimeout(() => location.reload(), 500);
                        }, 100);
                    } else {
                        showAlert('danger', data.message || 'Failed to add package');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    showAlert('danger', 'Error occurred while adding package');
                })
                .finally(() => {
                    // Restore button state
                    submitBtn.prop('disabled', false).text(originalText);
                });
        });

        // Edit Package
        function editPackage(packageId) {
            fetch(`/admin/billing/api/packages/${packageId}`)
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        const package = data.package;
                        $('#edit_package_id').val(package.id);
                        $('#edit_name').val(package.name);
                        $('#edit_speed').val(package.speed);
                        $('#edit_price').val(package.price);

                        // Set tax rate and checkbox based on package data
                        const taxRate = package.tax_rate || 0;
                        if (taxRate > 0) {
                            $('#edit_enable_tax').prop('checked', true);
                            $('#edit_tax_rate').val(taxRate);
                            $('#edit_tax_rate_group').show();
                            $('#edit_tax_rate_group input').prop('disabled', false);
                        } else {
                            $('#edit_enable_tax').prop('checked', false);
                            $('#edit_tax_rate').val(0);
                            $('#edit_tax_rate_group').hide();
                            $('#edit_tax_rate_group input').prop('disabled', true);
                        }

                        $('#edit_description').val(package.description || '');

                        // Calculate and display price with tax
                        calculatePriceWithTax('edit_price', 'edit_tax_rate', 'edit_price_with_tax');

                        // Show current image preview if exists
                        if (package.image_filename) {
                            const imagePreview = `<p><strong>Current Image:</strong></p>
                                                  <img src="/img/packages/${package.image_filename}" alt="${package.name}" style="max-width: 150px; height: auto; border-radius: 5px;">
                                                  <p class="text-muted mt-2">Leave empty if you don't want to change the image</p>`;
                            $('#current_image_preview').html(imagePreview);
                        } else {
                            $('#current_image_preview').html('<p class="text-muted">No image currently</p>');
                        }

                        // Load profiles then set selection and show modal
                        const selected = package.pppoe_profile || 'default';
                        loadPPPoEProfileeeeeeeeees('#edit_pppoe_profile', selected);
                        $('#editPackageModal').modal('show');
                    } else {
                        showAlert('danger', data.message || 'Failed to fetch package data');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    showAlert('danger', 'Error occurred');
                });
        }

        // Update Package Form
        $('#editPackageForm').on('submit', function (e) {
            e.preventDefault();

            // Get form data
            const packageId = $('#edit_package_id').val();
            const formData = new FormData();

            // Add text fields
            formData.append('name', $('#edit_name').val().trim());
            formData.append('speed', $('#edit_speed').val().trim());
            formData.append('price', parseFloat($('#edit_price').val()));
            formData.append('tax_rate', $('#edit_enable_tax').is(':checked') ? parseFloat($('#edit_tax_rate').val()) : 0);
            formData.append('description', $('#edit_description').val().trim());
            formData.append('pppoe_profile', $('#edit_pppoe_profile').val().trim() || 'default');

            // Add image file if selected
            const imageFile = $('#edit_package_image')[0].files[0];
            if (imageFile) {
                formData.append('package_image', imageFile);
            }

            // Validate required fields
            if (!formData.get('name') || !formData.get('speed') || !formData.get('price') || formData.get('price') <= 0) {
                showAlert('danger', 'Name, speed, and price must be filled correctly');
                return;
            }

            // Validate tax rate if enabled
            if ($('#edit_enable_tax').is(':checked') && (parseFloat(formData.get('tax_rate')) < 0 || parseFloat(formData.get('tax_rate')) > 100)) {
                showAlert('danger', 'VAT must be between 0% and 100%');
                return;
            }

            // Show loading state
            const submitBtn = $(this).find('button[type="submit"]');
            const originalText = submitBtn.text();
            submitBtn.prop('disabled', true).text('Processing...');

            fetch(`/admin/billing/packages/${packageId}`, {
                method: 'PUT',
                body: formData
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        showAlert('success', 'Package successfully updated');
                        // Close modal with a small delay to ensure toast is shown
                        setTimeout(() => {
                            $('#editPackageModal').modal('hide');
                            setTimeout(() => location.reload(), 500);
                        }, 100);
                    } else {
                        showAlert('danger', data.message || 'Failed to update package');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    showAlert('danger', 'Error occurred while updating package');
                })
                .finally(() => {
                    // Restore button state
                    submitBtn.prop('disabled', false).text(originalText);
                });
        });

        // View Package
        function viewPackage(packageId) {
            fetch(`/admin/billing/api/packages/${packageId}`)
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        const package = data.package;
                        const taxRate = package.tax_rate || 11;
                        const isNoTax = taxRate === 0;

                        let imageHtml = '';
                        if (package.image_filename) {
                            imageHtml = `<p><strong>Package Image:</strong></p>
                                         <img src="/img/packages/${package.image_filename}" alt="${package.name}" style="max-width: 200px; height: auto; border-radius: 5px;">`;
                        }

                        const detailsHtml = `
                            <div class="row">
                                <div class="col-md-6">
                                    <p><strong>Package Name:</strong> ${package.name}</p>
                                    <p><strong>Speed:</strong> ${package.speed}</p>
                                    <p><strong>Price (Before VAT):</strong> Rs ${parseFloat(package.price).toLocaleString('en-PK')}</p>
                                    <p><strong>VAT:</strong> 
                                        ${isNoTax ?
                                '<span class="badge bg-secondary">0% (No VAT)</span>' :
                                '<span class="badge bg-warning">' + taxRate.toFixed(2) + '%</span>'
                            }
                                    </p>
                                    <p><strong>Price (After VAT):</strong> 
                                        ${isNoTax ?
                                'Rs ' + parseFloat(package.price).toLocaleString('en-PK') + ' <small class="text-muted">(Same price, no VAT)</small>' :
                                'Rs ' + ((parseFloat(package.price) * (1 + taxRate / 100)).toFixed(0)).toLocaleString('en-PK')
                            }
                                    </p>
                                    <p><strong>PPPoE Profileeeeeeeeee:</strong> ${package.pppoe_profile || 'default'}</p>
                                </div>
                                <div class="col-md-6">
                                    <p><strong>Description:</strong></p>
                                    <p>${package.description || 'No description'}</p>
                                    ${imageHtml}
                                </div>
                            </div>
                        `;

                        $('#packageDetails').html(detailsHtml);
                        $('#viewPackageModal').modal('show');
                    } else {
                        showAlert('danger', data.message || 'Failed to fetch package data');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    showAlert('danger', 'Error occurred');
                });
        }

        // Delete Package
        function deletePackage(packageId) {
            if (confirm('Are you sure you want to delete this package?')) {
                fetch(`/admin/billing/packages/${packageId}`, {
                    method: 'DELETE'
                })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            showAlert('success', 'Package successfully deleted');
                            setTimeout(() => location.reload(), 1000);
                        } else {
                            showAlert('danger', data.message || 'Failed to delete package');
                        }
                    })
                    .catch(error => {
                        console.error('Error:', error);
                        showAlert('danger', 'Error occurred');
                    });
            }
        }

        function showToast(type, title, message) {
            const toastHtml = `
                <div class="toast align-items-center text-white bg-${type} border-0" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="d-flex">
                        <div class="toast-body">
                            <strong>${title}</strong><br>
                            ${message}
                        </div>
                        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                    </div>
                </div>
            `;

            // Remove existing toasts
            document.querySelectorAll('.toast').forEach(toast => toast.remove());

            // Add new toast
            document.body.insertAdjacentHTML('beforeend', toastHtml);

            // Show toast
            const toastElement = document.querySelector('.toast');
            const toast = new bootstrap.Toast(toastElement);
            toast.show();

            // Auto remove after 5 seconds
            setTimeout(() => {
                if (toastElement && toastElement.parentNode) {
                    toastElement.remove();
                }
            }, 5000);
        }

        function showAlert(type, message) {
            showToast(type === 'success' ? 'success' : type === 'danger' ? 'danger' : 'info',
                type === 'success' ? 'Success' : type === 'danger' ? 'Error' : 'Info',
                message);
        }
    </script>

    <!-- Currency Helper -->
    <%- include('../../partials/currency-helper') %>

        <!-- Mobile Bottom Navbar -->
        <%- include('../../partials/admin-mobile-navbar', { currentPage: 'customers' }) %>
</body>

</html>