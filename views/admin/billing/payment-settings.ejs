<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Gateway Settings - Admin Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.0.7/css/boxicons.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Admin Mobile Optimizations -->
    <link href="/css/admin-mobile.css" rel="stylesheet">
    <link href="/css/mobile-sidebar.css" rel="stylesheet">
    <link href="/css/responsive-admin.css" rel="stylesheet">
    <style>
        .gateway-card {
            transition: transform 0.3s ease;
        }

        .gateway-card:hover {
            transform: translateY(-5px);
        }

        .status-badge {
            font-size: 0.8rem;
        }
    </style>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Include Billing Sidebar -->
            <%- include('../../partials/billing-sidebar') %>

                <!-- Main content -->
                <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
                    <h3 class="mb-4">Settings</h3>

                    <!-- Tab Navigation -->
                    <ul class="nav nav-tabs mb-4">
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'payment-settings' ? 'active' : '' %>"
                                href="/admin/billing/payment-settings">
                                <i class="bi bi-cog"></i> Payment Settings
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'whatsapp-settings' ? 'active' : '' %>"
                                href="/admin/billing/whatsapp-settings">
                                <i class="bi bi-chat-dots"></i> WhatsApp Settings
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'service-suspension' ? 'active' : '' %>"
                                href="/admin/billing/service-suspension">
                                <i class="bi bi-pause-circle"></i> ISOLIR Settings
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'mapping-new' ? 'active' : '' %>"
                                href="/admin/billing/mapping-new">
                                <i class="bi bi-map"></i> Network Mapping
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof page !== 'undefined' && page === 'genieacs' ? 'active' : '' %>"
                                href="/admin/genieacs">
                                <i class="bi bi-wifi"></i> GenieACS
                            </a>
                        </li>
                    </ul>

                    <div
                        class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                        <h1 class="h2">
                            <i class="bx bx-cog"></i> Payment Gateway Settings
                        </h1>
                    </div>

                    <!-- Alert Messages -->
                    <div id="alertContainer"></div>

                    <!-- Active Gateway Selection -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="card">
                                <div class="card-header">
                                    <h5 class="card-title mb-0">
                                        <i class="bx bx-target-lock"></i> Active Gateway
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-md-6">
                                            <label class="form-label">Select Active Gateway:</label>
                                            <select class="form-select" id="activeGateway">
                                                <option value="midtrans" <%=settings.payment_gateway.active==='midtrans'
                                                    ? 'selected' : '' %>>Midtrans</option>
                                                <option value="xendit" <%=settings.payment_gateway.active==='xendit'
                                                    ? 'selected' : '' %>>Xendit</option>
                                                <option value="tripay" <%=settings.payment_gateway.active==='tripay'
                                                    ? 'selected' : '' %>>Tripay</option>
                                                <option value="duitku" <%=settings.payment_gateway.active==='duitku'
                                                    ? 'selected' : '' %>>Duitku</option>
                                            </select>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label">Gateway Status:</label>
                                            <div id="gatewayStatus">
                                                <span class="badge bg-success">Midtrans: Enabled (Active)</span>
                                                <span class="badge bg-secondary">Xendit: Disabled</span>
                                                <span class="badge bg-secondary">Tripay: Disabled</span>
                                                <span class="badge bg-secondary">Duitku: Disabled</span>
                                            </div>
                                        </div>
                                    </div>
                                    <button class="btn btn-primary mt-3" onclick="updateActiveGateway()">
                                        <i class="bx bx-save"></i> Save Active Gateway
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Gateway Configurations -->
                    <div class="row">
                        <!-- Midtrans Configuration -->
                        <div class="col-lg-4 mb-4">
                            <div class="card gateway-card">
                                <div class="card-header">
                                    <h5 class="card-title mb-0">
                                        <i class="bx bx-credit-card"></i> Midtrans
                                        <span
                                            class="badge <%= settings.payment_gateway.midtrans.enabled ? 'bg-success' : 'bg-secondary' %> status-badge">
                                            <%= settings.payment_gateway.midtrans.enabled ? 'Enabled' : 'Disabled' %>
                                        </span>
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <form id="midtransForm">
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="midtransEnabled"
                                                    <%=settings.payment_gateway.midtrans.enabled ? 'checked' : '' %>>
                                                <label class="form-check-label" for="midtransEnabled">
                                                    Enable Midtrans
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="midtransProduction"
                                                    <%=settings.payment_gateway.midtrans.production ? 'checked' : '' %>>
                                                <label class="form-check-label" for="midtransProduction">
                                                    Production Mode
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Server Key:</label>
                                            <input type="text" class="form-control" id="midtransServerKey"
                                                value="<%= settings.payment_gateway.midtrans.server_key %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Client Key:</label>
                                            <input type="text" class="form-control" id="midtransClientKey"
                                                value="<%= settings.payment_gateway.midtrans.client_key %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Merchant ID:</label>
                                            <input type="text" class="form-control" id="midtransMerchantId"
                                                value="<%= settings.payment_gateway.midtrans.merchant_id %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Base URL (Callback/Return):</label>
                                            <input type="text" class="form-control" id="midtransBaseUrl"
                                                placeholder="https://billing.yourdomain.com or http://IP:PORT"
                                                value="<%= (settings.payment_gateway.midtrans && settings.payment_gateway.midtrans.base_url) ? settings.payment_gateway.midtrans.base_url : '' %>">
                                            <div class="form-text">If empty, system uses host/port from settings.json</div>
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100">
                                            <i class="bx bx-save"></i> Save Midtrans
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>

                        <!-- Xendit Configuration -->
                        <div class="col-lg-4 mb-4">
                            <div class="card gateway-card">
                                <div class="card-header">
                                    <h5 class="card-title mb-0">
                                        <i class="bx bx-credit-card"></i> Xendit
                                        <span
                                            class="badge <%= settings.payment_gateway.xendit.enabled ? 'bg-success' : 'bg-secondary' %> status-badge">
                                            <%= settings.payment_gateway.xendit.enabled ? 'Enabled' : 'Disabled' %>
                                        </span>
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <form id="xenditForm">
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="xenditEnabled"
                                                    <%=settings.payment_gateway.xendit.enabled ? 'checked' : '' %>>
                                                <label class="form-check-label" for="xenditEnabled">
                                                    Enable Xendit
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="xenditProduction"
                                                    <%=settings.payment_gateway.xendit.production ? 'checked' : '' %>>
                                                <label class="form-check-label" for="xenditProduction">
                                                    Production Mode
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">API Key:</label>
                                            <input type="text" class="form-control" id="xenditApiKey"
                                                value="<%= settings.payment_gateway.xendit.api_key %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Callback Token:</label>
                                            <input type="text" class="form-control" id="xenditCallbackToken"
                                                value="<%= settings.payment_gateway.xendit.callback_token %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Base URL (Redirect):</label>
                                            <input type="text" class="form-control" id="xenditBaseUrl"
                                                placeholder="https://billing.yourdomain.com or http://IP:PORT"
                                                value="<%= (settings.payment_gateway.xendit && settings.payment_gateway.xendit.base_url) ? settings.payment_gateway.xendit.base_url : '' %>">
                                            <div class="form-text">If empty, system uses host/port from settings.json</div>
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100">
                                            <i class="bx bx-save"></i> Save Xendit
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>

                        <!-- Tripay Configuration -->
                        <div class="col-lg-4 mb-4">
                            <div class="card gateway-card">
                                <div class="card-header">
                                    <h5 class="card-title mb-0">
                                        <i class="bx bx-credit-card"></i> Tripay
                                        <span
                                            class="badge <%= settings.payment_gateway.tripay.enabled ? 'bg-success' : 'bg-secondary' %> status-badge">
                                            <%= settings.payment_gateway.tripay.enabled ? 'Enabled' : 'Disabled' %>
                                        </span>
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <form id="tripayForm">
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="tripayEnabled"
                                                    <%=settings.payment_gateway.tripay.enabled ? 'checked' : '' %>>
                                                <label class="form-check-label" for="tripayEnabled">
                                                    Enable Tripay
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="tripayProduction"
                                                    <%=settings.payment_gateway.tripay.production ? 'checked' : '' %>>
                                                <label class="form-check-label" for="tripayProduction">
                                                    Production Mode
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">API Key:</label>
                                            <input type="text" class="form-control" id="tripayApiKey"
                                                value="<%= settings.payment_gateway.tripay.api_key %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Private Key:</label>
                                            <input type="text" class="form-control" id="tripayPrivateKey"
                                                value="<%= settings.payment_gateway.tripay.private_key %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Merchant Code:</label>
                                            <input type="text" class="form-control" id="tripayMerchantCode"
                                                value="<%= settings.payment_gateway.tripay.merchant_code %>">
                                        </div>
                                        <div class="mb-3">
                                            <div class="alert alert-info">
                                                <i class="bx bx-info-circle"></i>
                                                <strong>Info:</strong> Tripay payment methods (QRIS, DANA, GoPay, etc.)
                                                are now selected directly by customers during payment.
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Base URL (Callback/Return):</label>
                                            <input type="text" class="form-control" id="tripayBaseUrl"
                                                placeholder="https://billing.yourdomain.com or http://IP:PORT"
                                                value="<%= (settings.payment_gateway.tripay && settings.payment_gateway.tripay.base_url) ? settings.payment_gateway.tripay.base_url : '' %>">
                                            <div class="form-text">Must start with http:// or https://</div>
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100">
                                            <i class="bx bx-save"></i> Save Tripay
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>

                        <!-- Duitku Configuration -->
                        <div class="col-lg-4 mb-4">
                            <div class="card gateway-card">
                                <div class="card-header">
                                    <h5 class="card-title mb-0">
                                        <i class="bx bx-credit-card"></i> Duitku
                                        <span
                                            class="badge <%= settings.payment_gateway.duitku && settings.payment_gateway.duitku.enabled ? 'bg-success' : 'bg-secondary' %> status-badge">
                                            <%= settings.payment_gateway.duitku && settings.payment_gateway.duitku.enabled ? 'Enabled' : 'Disabled' %>
                                        </span>
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <form id="duitkuForm">
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="duitkuEnabled"
                                                    <%=settings.payment_gateway.duitku && settings.payment_gateway.duitku.enabled ? 'checked' : '' %>>
                                                <label class="form-check-label" for="duitkuEnabled">
                                                    Enable Duitku
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" id="duitkuProduction"
                                                    <%=settings.payment_gateway.duitku && settings.payment_gateway.duitku.production ? 'checked' : '' %>>
                                                <label class="form-check-label" for="duitkuProduction">
                                                    Production Mode
                                                </label>
                                            </div>
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Merchant Code:</label>
                                            <input type="text" class="form-control" id="duitkuMerchantCode"
                                                value="<%= settings.payment_gateway.duitku && settings.payment_gateway.duitku.merchant_code ? settings.payment_gateway.duitku.merchant_code : '' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">API Key (Merchant Key):</label>
                                            <input type="text" class="form-control" id="duitkuApiKey"
                                                value="<%= settings.payment_gateway.duitku && settings.payment_gateway.duitku.api_key ? settings.payment_gateway.duitku.api_key : '' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Default Payment Method (Kode):</label>
                                            <input type="text" class="form-control" id="duitkuMethod"
                                                placeholder="VC / BC / M2 / I1 / BT / OV / SP / LA"
                                                value="<%= settings.payment_gateway.duitku && settings.payment_gateway.duitku.method ? settings.payment_gateway.duitku.method : '' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Expiry Period (minutes):</label>
                                            <input type="number" class="form-control" id="duitkuExpiryPeriod" min="1" max="1440"
                                                value="<%= settings.payment_gateway.duitku && settings.payment_gateway.duitku.expiry_period ? settings.payment_gateway.duitku.expiry_period : '' %>">
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Base URL (Callback/Return):</label>
                                            <input type="text" class="form-control" id="duitkuBaseUrl"
                                                placeholder="https://billing.yourdomain.com or http://IP:PORT"
                                                value="<%= (settings.payment_gateway.duitku && settings.payment_gateway.duitku.base_url) ? settings.payment_gateway.duitku.base_url : '' %>">
                                            <div class="form-text">If empty, system uses host/port from settings.json</div>
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100">
                                            <i class="bx bx-save"></i> Save Duitku
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Test Connection Section -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <div class="card">
                                <div class="card-header">
                                    <h5 class="card-title mb-0">
                                        <i class="bx bx-test-tube"></i> Gateway Connection Test
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-md-3">
                                            <button class="btn btn-outline-primary w-100"
                                                onclick="testGateway('midtrans')">
                                                <i class="bx bx-test-tube"></i> Test Midtrans
                                            </button>
                                        </div>
                                        <div class="col-md-3">
                                            <button class="btn btn-outline-primary w-100"
                                                onclick="testGateway('xendit')">
                                                <i class="bx bx-test-tube"></i> Test Xendit
                                            </button>
                                        </div>
                                        <div class="col-md-3">
                                            <button class="btn btn-outline-primary w-100"
                                                onclick="testGateway('tripay')">
                                                <i class="bx bx-test-tube"></i> Test Tripay
                                            </button>
                                        </div>
                                        <div class="col-md-3">
                                            <button class="btn btn-outline-primary w-100"
                                                onclick="testGateway('duitku')">
                                                <i class="bx bx-test-tube"></i> Test Duitku
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function showAlert(type, message) {
            const alertContainer = document.getElementById('alertContainer');
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert alert-${type} alert-dismissible fade show`;
            alertDiv.innerHTML = `
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;
            alertContainer.appendChild(alertDiv);

            setTimeout(() => {
                alertDiv.remove();
            }, 5000);
        }

        async function updateActiveGateway() {
            const activeGateway = document.getElementById('activeGateway').value;

            try {
                const response = await fetch('/admin/billing/payment-settings/active-gateway', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ activeGateway })
                });

                const result = await response.json();

                if (result.success) {
                    showAlert('success', 'Active gateway updated successfully');
                    updateGatewayStatus();
                } else {
                    showAlert('danger', result.message || 'Failed to update active gateway');
                }
            } catch (error) {
                console.error('Error:', error);
                showAlert('danger', 'Error occurred while updating active gateway');
            }
        }

        async function updateGatewayStatus() {
            try {
                const response = await fetch('/payment/gateway-status');

                const result = await response.json();

                if (result.success) {
                    const status = result.data;
                    let statusHtml = '';

                    Object.keys(status).forEach(gateway => {
                        const isEnabled = status[gateway].enabled;
                        const isActive = status[gateway].active;
                        const statusClass = isEnabled ? 'success' : 'secondary';
                        const statusText = isEnabled ? 'Enabled' : 'Disabled';
                        const activeText = isActive ? ' (Active)' : '';

                        statusHtml += `
                            <span class="badge bg-${statusClass} me-2">
                                ${gateway.charAt(0).toUpperCase() + gateway.slice(1)}: ${statusText}${activeText}
                            </span>
                        `;
                    });

                    document.getElementById('gatewayStatus').innerHTML = statusHtml;
                }
            } catch (error) {
                console.error('Error updating gateway status:', error);
            }
        }

        // Form submission handlers
        document.getElementById('midtransForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            await saveGatewayConfig('midtrans');
        });

        document.getElementById('xenditForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            await saveGatewayConfig('xendit');
        });

        document.getElementById('tripayForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            await saveGatewayConfig('tripay');
        });

        document.getElementById('duitkuForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            await saveGatewayConfig('duitku');
        });

        async function saveGatewayConfig(gateway) {
            const formData = {
                enabled: document.getElementById(`${gateway}Enabled`).checked,
                production: document.getElementById(`${gateway}Production`).checked
            };

            // Add gateway-specific fields
            if (gateway === 'midtrans') {
                formData.server_key = document.getElementById('midtransServerKey').value;
                formData.client_key = document.getElementById('midtransClientKey').value;
                formData.merchant_id = document.getElementById('midtransMerchantId').value;
                const baseUrl = document.getElementById('midtransBaseUrl').value.trim();
                if (baseUrl && !/^https?:\/\//i.test(baseUrl)) {
                    showAlert('danger', 'Base URL must start with http:// or https://');
                    return;
                }
                if (baseUrl) formData.base_url = baseUrl;
            } else if (gateway === 'xendit') {
                formData.api_key = document.getElementById('xenditApiKey').value;
                formData.callback_token = document.getElementById('xenditCallbackToken').value;
                const baseUrl = document.getElementById('xenditBaseUrl').value.trim();
                if (baseUrl && !/^https?:\/\//i.test(baseUrl)) {
                    showAlert('danger', 'Base URL must start with http:// or https://');
                    return;
                }
                if (baseUrl) formData.base_url = baseUrl;
            } else if (gateway === 'tripay') {
                formData.api_key = document.getElementById('tripayApiKey').value;
                formData.private_key = document.getElementById('tripayPrivateKey').value;
                formData.merchant_code = document.getElementById('tripayMerchantCode').value;
                const baseUrl = document.getElementById('tripayBaseUrl').value.trim();
                if (baseUrl && !/^https?:\/\//i.test(baseUrl)) {
                    showAlert('danger', 'Base URL must start with http:// or https://');
                    return;
                }
                if (baseUrl) formData.base_url = baseUrl;
            } else if (gateway === 'duitku') {
                formData.merchant_code = document.getElementById('duitkuMerchantCode').value;
                formData.api_key = document.getElementById('duitkuApiKey').value;
                formData.method = document.getElementById('duitkuMethod').value;
                formData.expiry_period = document.getElementById('duitkuExpiryPeriod').value;
                const baseUrl = document.getElementById('duitkuBaseUrl').value.trim();
                if (baseUrl && !/^https?:\/\//i.test(baseUrl)) {
                    showAlert('danger', 'Base URL must start with http:// or https://');
                    return;
                }
                if (baseUrl) formData.base_url = baseUrl;
            }

            try {
                const response = await fetch(`/admin/billing/payment-settings/${gateway}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();

                if (result.success) {
                    showAlert('success', `${gateway} configuration saved successfully`);
                    updateGatewayStatus();
                } else {
                    showAlert('danger', result.message || `Failed to save ${gateway} configuration`);
                }
            } catch (error) {
                console.error('Error:', error);
                showAlert('danger', `Error occurred while saving ${gateway} configuration`);
            }
        }

        async function testGateway(gateway) {
            try {
                const response = await fetch(`/admin/billing/payment-settings/test/${gateway}`, {
                    method: 'POST'
                });

                const result = await response.json();

                if (result.success) {
                    showAlert('success', `${gateway} connection test successful: ${result.message}`);
                } else {
                    showAlert('danger', `${gateway} connection test failed: ${result.message}`);
                }
            } catch (error) {
                console.error('Error:', error);
                showAlert('danger', `Error occurred during ${gateway} connection test`);
            }
        }

        // Initialize page
        document.addEventListener('DOMContentLoaded', function () {
            updateGatewayStatus();
        });
    </script>

    <!-- Mobile Bottom Navbar -->
    <%- include('../../partials/admin-mobile-navbar', { currentPage: 'settings' }) %>
</body>

</html>
